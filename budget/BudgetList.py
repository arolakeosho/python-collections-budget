class budgetlist(pass):
    """docstring fs budgetlist.passdef __init__(self, budget):
        sups budgetlist,pass.__init__()
        self.budget = budgetlist
        self.sum_expenses=0
        self.sum_overages=0
        self.overages= []
         self.append(pass)
         item.append(pass)
         if self.sum_expenses
